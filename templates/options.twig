<div class="modal fade pr-0" id="optionsModal" tabindex="-1" role="dialog" aria-hidden="true" data-backdrop="static">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Choose A Filter</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body p-0">
                <div class="container-fluid">
                    <label for="filter" class="label-danger">Select a filter you would like to use.</label>
                    <select name="filter" class="form-control filter" id="filter">

                    </select>
                    <div class="invisible">
                        <label> Custom Filter
                            <input id="custom-filter" type="hidden">
                        </label>
                    </div>
                </div>
            </div>
            <div class="modal-footer d-flex justify-content-between">
                <div class="d-flex align-items-center flex-row">

                </div>
                <button type="button" class="btn btn-primary" id="modal-fetch">Continue</button>
            </div>
        </div>
    </div>
</div>